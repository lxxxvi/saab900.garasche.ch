!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Saab 900 ADMIN
    = csrf_meta_tags
    = csp_meta_tag
    = javascript_pack_tag 'application'
    = stylesheet_pack_tag 'application'
  %body
    .m-4
      .text-xl.text-red-800.border-b.mb-8 Saab 900 Admin

      - if admin_signed_in?
        .flex
          .w-full.text-right
            = link_to t('general.sign_out'),
                      admin_session_path,
                      method: :delete,
                      class: 'button button-red-hollow text-xs'

      - if flash.any?
        - flash.each do |key, message|
          .flash{ class: "flash-#{key}"}= message

      = yield
